<p>Photos!</p>
<ul class='photo-gallery'>
<% @person.each do |person| %>
  <% if person.photo_file_name.present? %>
    <li><%= image_tag person.photo.url %><span><%= person.first_name %> <%= person.last_name %></span></li>
  <% end %>
<% end %>
</ul>
